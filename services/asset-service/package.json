{
  "scripts": {
    "start": "platformatic start",
    "test": "tsc && node --test dist/test/*/*.test.js",
    "clean": "rm -fr ./dist",
    "build": "platformatic compile",
    "migrate": "platformatic db migrations apply",
    "seed": "platformatic db seed seed.js"
  },
  "devDependencies": {
    "fastify": "^4.24.2",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "@platformatic/db": "^1.4.1",
    "platformatic": "^1.4.1"
  },
  "engines": {
    "node": "^18.8.0 || >=20.6.0"
  }
}
